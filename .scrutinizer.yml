build:
    environment:
        php: '8.3'
    nodes:
        analysis:
            environment:
                php: '8.3'
            tests:
                override:
                    - php-scrutinizer-run
